const tellFurtune = function (
  jobTitle,
  location,
  partnerName,
  numberOfChildren
) {
  console.log(`You will be a ${jobTitle} in ${location} married to ${partnerName}
  with ${numberOfChildren} children.`);
};

tellFurtune("programmer", "Israel", "Mais", 1);
